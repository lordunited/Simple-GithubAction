name: Build Docker Container
on:
  push:
    branches:
      - main
      - master 
jobs:
  docker:
    runs-on: stage
    steps:
      -
        name: build 
        uses: docker build -t registry.dosma.ir/stage/backend:${{github.sha}} .	
